defmodule Todo.List do
  def new(entries \\ []) do
  end

  def size(todo_list) do
  end

  def add_entry(todo_list, entry) do
  end

  def entries(todo_list, date) do
  end

  def update_entry(todo_list, %{} = new_entry) do
  end

  def delete_entry(todo_list, %{} = new_entry) do
  end
end
